<tr>
    <th>
        <label for="{{name}}">{{label}}</label>
    </th>
    <td>
        <input type="checkbox" name="{{name}}" id="{{name}}" value="1" {{#isChecked}}checked{{/isChecked}}>
        {{reminderText}}
    </td>
</tr>
