<div id="gatherling_main" class="box grid_12">
    <h1 class="uppertitle">Gatherling</h1>
    <p>
        Welcome to Gatherling! The place for player-run Magic Online tournaments.
        Report bugs or give feedback on the <a href="https://discord.gg/F9SrMwV">Discord</a>.
    </p>
</div>
<div id="maincolumn" class="grid_8">
    <div class="box">
        <h2 class="uppertitle">Active Events</h1>
        {{#hasActiveEvents}}
            <table class="events">
                {{#activeEvents}}
                    <tr>
                        <td>
                            <a href="{{eventReportLink}}">{{name}}</a><br>
                            {{format}}
                        </td>
                        <td style="text-align: right">Round {{currentRound}}</td>
                    </tr>
                {{/activeEvents}}
            </table>
        {{/hasActiveEvents}}
    </div>
    <div class="box">
        <h2 class="uppertitle">Upcoming Events</h1>
        {{#hasUpcomingEvents}}
            <table class="events">
                {{#upcomingEvents}}
                    <tr>
                        <td>
                            <a href="{{eventReportLink}}">{{name}}</a><br>
                            {{format}}
                        </td>
                        <td style="text-align: right">
                            {{#time}}{{> time}}{{/time}}
                        </td>
                    </tr>
                {{/upcomingEvents}}
            </table>
        {{/hasUpcomingEvents}}
    </div>
    <div class="box">
        <h2 class="uppertitle">Gatherling Statistics</h1>
        {{#stats}}
            <ul>
                <li>There are {{decks}} unique decks.</li>
                <li>We have recorded {{matches}} matches from {{events}} events.</li>
                <li>There are {{activePlayers}} active players in Gatherling. ({{verifiedPlayers}} verified.)</li>
            </ul>
        {{/stats}}
    </div>
</div>
<div class="grid_4">
    <div class="box pad">
        {{#playerInfo}}
            <div>
                <p><b>Welcome back {{name}}</b></p>
                <ul>
                    <li> <a href="profile.php">Check out your profile</a> </li>
                    <li> <a href="player.php?mode=alldecks">See your decklists</a> </li>
                    {{#mostRecentHostedEvent}}
                        <li><a href="{{link}}">Manage {{name}}</a></li>
                    {{/mostRecentHostedEvent}}
                    {{#isHost}}
                        <li> <a href="event.php">Host Control Panel</a></li>
                    {{/isHost}}
                </ul>
            </div>
        {{/playerInfo}}
        {{^player}}
            <h2 class="uppertitle">Login to Gatherling</h1>
            <form action="login.php" method="post">
                <table class="form">
                    <tr>
                        <th><label for="username">Username</label></th>
                        <td><input class="inputbox" type="text" name="username" value=""></td>
                    </tr>
                    <tr>
                        <th><label for="password">Password</label></th>
                        <td><input class="inputbox" type="password" name="password" value=""></td>
                    </tr>
                    <tr>
                        <td colspan="2" class="buttons">
                            <input class="inputbutton btn-primary" type="submit" name="mode" value="Log In">
                            <button class="inputbutton discordlogin" name="mode" value="Log In with Discord">
                                <i class="fab fa-discord"></i> Log In with Discord
                            </button>
                        </td>
                    </tr>
                </table>
            </form>
            <p>
                <a href="register.php">Need to register?</a><br>
                <a href="forgot.php">Forgot your password?</a>
            </p>
        {{/player}}
    </div>
    <div class="box">
        <h2 class="uppertitle">Recent Winners</h1>
            {{#hasRecentWinners}}
                <table class="winners">
                    {{#recentWinners}}
                        <tr>
                            <td class="event-name" colspan="2">{{eventName}}</td>
                        </tr>
                        <tr>
                            <td colspan="2">
                                <a class="borderless" href="{{eventReportLink}}">
                                    {{#colorImages}}{{> colorImages}}{{/colorImages}}
                                </a>
                            </td>
                        </tr>
                        <tr class="player-deck">
                            <td>
                                <b><a href="{{playerLink}}">{{playerName}}</a></b>
                            </td>
                            <td>
                                <i><a href="{{deckLink}}">{{deckName}}</a></i>
                            </td>
                        </tr>
                    {{/recentWinners}}
                </table>
            {{/hasRecentWinners}}
    </div>
</div>
